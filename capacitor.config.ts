import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rajguru.app',
  appName: 'RajGuru',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
